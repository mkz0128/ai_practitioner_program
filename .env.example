# Copy this file to .env and replace the placeholder. Never commit the real .env file.
AUCTION_AGENT_MODE=openai
OPENAI_MODEL=gpt-5.5
OPENAI_API_KEY=replace-with-your-key
# Local demo membership token. Replace with a random value; never commit .env.
DEMO_MEMBER_TOKEN=replace-with-a-random-member-token
